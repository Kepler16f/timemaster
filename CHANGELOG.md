# 更新记录

版本号只在 `public/app.js` 的 `APP_VERSION` 定义一次。每次发版前在顶部新增一节，
CI 的 `scripts/release-notes.sh` 会把对应小节原样写进 GitHub Release 说明；
跨版本补发时（上一个 Release 之后攒了好几版）会把中间几节一起带上，
找不到小节时退回「相较上一个标签的提交记录」。

## v0.2.5

这一版修的是多人共用一个配置码时的成员与数据同步，另外鸿蒙不再每开一次就多一个后台卡片。

- 修复 **三个人（及更多）用同一个配置码时，最后加入的人「查无此人」**：不创建日程就看不到他的成员标签、空间里也一直显示 2 人。根因是网盘没有事务，谁写回都是整篇 JSON 覆盖，只要有一端拿着过期的文档写回，就会把别人刚加进去的成员（连带日程）抹掉，而被抹掉的人自己并不会补回来
- 改动 **同步引擎：成员名单只增不减 + 自愈**。本机记一份「见过的成员」留档，每次合并都补回去；发现自己不在云端名单里就自动重新登记并写回，被旧版本覆盖掉的人一两轮同步内就会回来
- 改动 **写回云端前一定先拉一次全量再合并**（之前只在拿不到 etag 时才这样），不再拿 304 的缓存整篇覆盖别人的数据
- 修复 **同一个人显示成两个成员标签**：换过身份键（登录/退出邮箱、设备号被原生存储接管、网页存储被系统清过）会留下旧键那一条，旧键名下没有日程，看着就像空间里多了个人。现在按「同设备 + 同昵称」合并成一条（老数据没设备号时按同昵称 + 同颜色判），只折掉名下零日程的重复项，本机自己那条绝不动，两条都有日程的也不动
- 修复 **应用内更新只有「检查到新版本」却下不动也装不了**：下载与安装两个按钮是用 `.hidden` 类藏起来的，代码却只切了 `hidden` 属性，类名一直留在按钮上，所以它们永远显示不出来。现在按钮正常出现，进度百分比直接采用原生侧算好的值
- 修复 **旧版本残留的安装包会被当成「已就绪」去装**：只有下载包的版本和最新一致才算就绪，过期的自动清掉重新下
- 修复 **每点一次下载就多挂一个进度监听**（进度会重复回调、越点越乱），现在只绑一次
- 修复 **创建空间时邀请码撞车会把别人的空间覆盖掉**：现在改为仅新建，撞到就提示重新创建
- 修复 **自己建的日程点开却提示「别人创建的无法删除」**：同一个人换过身份键后，旧键名下那几条就成了「不是我的」，标签看着是自己、详情里却锁死。现在本机历史身份键一律算自己（旧键名下的日程还带设备号的话，同步时直接并到当前身份上），改、删、批量管理都按这个判定；空间改名权限同理
- 新增 **周视图一屏四格 / 七格切换**：小时列上方原来的「时」位置换成展开收起按钮，点一下把整周压进一屏（列宽按实测宽度算，不会多出几像素的横向滑动），再点恢复一屏四格，偏好会被记住；平板宽度仍是整周平铺，该位置保持显示「时」
- 新增 **设置-外观的「日视图」开关，默认关闭**：不常用的人视图条上不再出现「日」这一格，周视图点表头也不会误入；打开开关后功能照旧，若当前正停在日视图则自动回到周视图
- 新增 **空间被创建者删除时的提示**：使用者下次同步会看到「该空间已经被创建者删除」，确认后连本机副本一起移除（要连续两轮都拉到 404 才判定，换网盘账号、网络抖动不会误删；本机还有待同步改动的空间也不会被误判）
- 修复 **切换空间面板点「返回」跳到了另一个空间**：直接点返回就应该留在当前空间，只是关掉面板（原来会回退到上一次进入的空间，这个「回退栈」语义已被移除）
- 修复 **鸿蒙端每点一次桌面图标就多一张最近任务卡片**：`EntryAbility` 改为单实例（`launchType: singleton`）

## v0.2.4

这一版是按真机截图逐项微调的：手机横屏终于能看到时间轴了，选择框也找回来了。

- 改动 **日视图表头压成一行**：`今天` 徽标 + `9月23日` + `周三` 横排，设备太窄放不下时自动折成两行（周视图仍是「星期在上、日期在下」）
- 修复 **手机横屏时底部留白过大、时间轴几乎看不见**：时间轴模式下外层预留的高度从 96px 压到 54px，正好贴着横屏变矮后的底栏，可视时间多了约五分之一
- 改动 **月历「收起/展开」按钮改成纯图标**（`⌃` / `⌄`），去掉文字后按钮从 108px 缩到 40px
- 删除 **设置-空间管理里的「进入」按钮**：整张卡片本来就可点，按钮是多余的
- 修复 **批量管理的复选框在深色模式下完全看不见**：全局 `input` 规则清掉了原生外观，现在整个框自己画（描边 + 勾选态填主色 + 对勾）
- 修复 **批量管理里选择框和日程文字挤成两行**：根因是 `.modal label` 优先级高于 `.batch-row`，把行方向覆盖成了 `column`，选择器带上 `.modal` 后压回一行；标题过长不再截省略号，改为居左、可向右滑动看全
- 改动 **批量管理不显示滚动条**：列表竖向和标题横向都只留滑动手势，不给滚动条留宽度
- 删除 **三处冗余说明文字**：网盘「测试连接」下方的路径说明、系统日历按钮下方的用法说明、关于-版本下方的署名说明
- 改动 **空间卡片改为两行**：第一行「房间名称 + 使用中」（名称单行超出省略），第二行才是「☁ 清空 / 移除」按钮
- 删除 **顶栏最右边的圆形「↩」按钮**（v0.2.3 加的那个）：切空间面板里的「返回」仍然回到上一个空间，功能没丢，只是不占顶栏一格
- 改动 **周视图打开即把今天滑到最左**：今天那一格自动贴到小时列右边第一格（周四就滑到周四打头），今天正好是周日时本就在第一格、不滑动。顺带修了冷启动时空间条量不到宽度导致定位失效的问题（先显示屏幕再渲染日历）
- 改动 **日历页顶部空间条分两行**：第一行只有空间标题，第二行是「邀请码 点击复制」+「同步」+「同步中…」三样并排；标题再长也只省略自己，不再把按钮挤窄。手机横屏（矮屏）仍收回一行，免得把刚还给时间轴的高度吃掉
- 改动 **平板（≥600px）日/周视图行高 58px → 78px**：宽屏上不再一行日程都塞不下

## v0.2.3

周/日时间轴这一版重点是「看得下、滑得动」，另外把系统日历回写的归属标清楚了。

- 改动 **写入系统日历的日程注明创建者**：标题后缀 `（成员名）`，描述里另起一行 `[Reunion] 创建者：成员名`；再从这个系统日历导入时按这枚标记跳过，不会把我们自己写回去的条目又抓回空间里
- 改动 **周视图一屏四天**：每格宽度按 `(屏宽 - 小时列) / 4` 计算，每小时行高 46px → 58px，横向滑动仍能看到完整七天
- 修复 **手机端周/日视图第二次进入划不到顶、星期表头被吃掉一半**：时间轴模式下外层 `#calMain` 是 `overflow:hidden`，之前却被程序滚到了当前小时，一旦滚走就再也滑不回来；现在外层强制归零，只让时间轴自己滚
- 新增 **日/周视图的「全天」行跟星期表头一起吸顶**：滚动 24 小时刻度时，全天日程和班休标记不再跟着滑走
- 新增 **时间标尺上显示精确到分钟的当前时间**：小时列是吸左的，横向滑动时也不会丢，每分钟自己刷新
- 改动 **周视图标题的日期区间拆两行**：第一个日期和 `–` 一行、第二个日期一行（日视图仍是一行），不再被顶栏挤断
- 修复 **切换空间的「返回」会掉回开屏页**：只有一个空间时改为留在当前空间；顶栏「☰」旁加了同款圆形「↩」按钮，只有存在上一个空间时才出现
- 改动 **设置-空间管理**：「使用中」徽标右对齐，邀请码那行的「码」字去掉
- 新增 **月视图可折叠**：手机上滑收起整月只留选中那一行、下滑展开，日历下方加了「⌃ 收起 / ⌄ 展开」按钮；平板宽度不折叠
- 改动 **成员条压成一行**：人多时横向滑动而不换行，不再竖向挤占日历空间

## v0.2.2

鸿蒙写系统日历的报错定位到了，另外把返回手势、自动旋转和日程编辑补齐。

- 修复 **鸿蒙端导入/回写系统日历报 `[23900001] invalid arg[0], i.e. invalid keys!`**：CalendarKit 的 `EventFilter` 是个带静态工厂的类，原生侧按包装对象解参，之前直接传 `{ timeRange, calendarType }` 对象字面量必然失败；改成 `calendarManager.EventFilter.filterByTime(起, 止)`
- 改动 **日/周视图的日程色块显示更多文字**：字号调大，仍是标题在前、时间在后，文字超出块高度时在块内上下滑动即可看完；一小时以内的短日程改成「标题 · 时间」同行挤着显示
- 新增 **自己的日程可以编辑**：详情弹窗在「删除」旁加「编辑」，改标题/日期/时间/类型/重复/地点/备注都是原地更新那一条（不会再冒出一条新日程），把「重复」改回不重复也会真的清掉循环规则
- 修复 **输入法收起后底部导航栏不回来**：鸿蒙收起输入法时不一定派发焦点事件，改成按可视视口被压矮与否判断，键盘一退底栏立刻恢复；转屏不会被误判成键盘
- 新增 **全面屏手势返回逐层回退**：侧滑/返回键依次关掉弹窗、退回日历页，只有在首页再返回才退出应用（Android 与鸿蒙都接上了）
- 改动 **手机端适配自动旋转**：鸿蒙 ability 明确为跟随系统旋转开关，Android 侧同样显式声明，横屏布局沿用上一版的适配
- 改动 **月视图日程卡列表的成员名尽量完整显示**（名称列 52px → 92px），右侧时间跨度条相应收窄压矮

## v0.2.1

平板与周视图的收尾，外加鸿蒙的深色模式。

- 修复 **鸿蒙端「深色模式跟随系统」无效**：ArkWeb 的 Web 组件默认不带网页深色模式，网页里的 `prefers-color-scheme` 永远收到浅色，改成跟随系统（`WebDarkMode.Auto`）后才真正联动；同时补了深色的启动窗口底色与 Web 组件底色，旋转/缩放时不再露出白边
- 改动 **周视图一格放大**：手机上一次显示 5 天（原来 7 天挤在一起放不下日程标题），左右滑动看满七天；小时列吸在左侧、日期表头吸在顶部，横着滑也不会看不出现在是几点
- 改动 时间轴改由自己滚动，右下角「新建」悬浮按钮不再白吃掉一截高度
- 修复 **平板竖屏/横屏月视图左右错位**：右侧日程卡列表会把左边的月历整块顶下去，现在两栏顶边对齐
- 改动 **月视图格子大小自适应**：宽屏下六行按剩余高度均分，平板上格子明显变大
- 改动 宽屏内容宽度上限 680px → 900px（≥1000px 时为 1120px）

## v0.2.0

界面重做的一版，日历终于像个日历了。

- 新增 **日 / 周 / 月三种视图**，选择会被记住，下次打开还是它
  - 月视图：格子只留日期与成员色块，一屏放得下整月；点某天在下方展开当天的日程卡列表（创建人、时间、地点、备注，按时间先后），列表顶部是「每人一条时间跨度轨」，谁排到几点一眼可见；「新建日程」收进列表最下面
  - 周视图：7 列时间轴，重叠日程自动分栏并排
  - 日视图：单列时间轴 + 全天区 + 当前时间红线（每分钟自己走）
- 新增 **横屏适配**：顶栏、格子、时间轴行高都按横屏压缩，不再是竖屏内容硬旋转
- 新增 **冷启动回到上次进入的空间**（首次安装仍进引导页）
- 改动 顶栏空间条：空间名加大、同步状态移到标题下方、同步按钮靠最右、邀请码一行显示；点空间名即可在弹出列表里快速切换空间（列表末尾含新建空间）
- 改动 切换空间面板与设置页功能对齐，可直接新建空间或用邀请码加入；「返回」回到上一次进入的空间
- 改动 设置页的空间卡片点一下就能切过去
- 改动 底栏「房间」更名为「空间」
- 修复 底栏遮挡主界面内容
- 移除 鸿蒙版的应用内更新入口（HAP 无法自装，留着只会误导）

## v0.1.5

- 新增 应用内更新：检查 GitHub Releases → 原生后台下载 APK → 唤起系统安装，下载进度可见
- 改动 月视图格子放大，日程标题可显示多行
- 修复 设备身份在清缓存后漂移的问题（身份改由原生侧持久化）
- 修复 同步状态误报

## v0.1.4

- 修复 鸿蒙创建空间 / 连接网盘失败（空响应体时 base64 解码崩溃）
- 新增 鸿蒙导航条沉浸式

## v0.1.3

- 修复 鸿蒙桥自检误报「桥未注入」（`runJavaScript` 返回值带 JSON 引号未剥离）

## v0.1.2

- 修复 鸿蒙 `registerJavaScriptProxy` 晚于首屏加载导致桥不可用（自检后自动重载）
- 修复 Android 主题图标缺省
- 改动 键盘弹起时底栏行为

## v0.1.1

- 新增 日程批量管理与多选删除
- 新增 网盘数据一键清空（带二次确认）
- 新增 邮箱验证码绑定到当前本地账户
- 修复 切换空间面板「返回」未回到上一次空间
- 新增 鸿蒙端启动自检浮层

## v0.1.0

- 更名 Reunion，重做图标与底栏药丸样式
- 修复 系统日程重复导入（按 `sourceUid` 去重 + 历史重复自动清理）

## v0.0.x

- 核心：坚果云 WebDAV 同步（免服务器）、多空间、成员标签色、循环日程（RRULE）、.ics 导入
- 双端壳：Capacitor Android APK + HarmonyOS NEXT HAP，全部由 GitHub Actions 产出
- 系统日历双向同步、深色模式、可选邮箱登录
