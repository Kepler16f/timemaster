# 更新记录

版本号只在 `public/app.js` 的 `APP_VERSION` 定义一次。每次发版前在顶部新增一节，
CI 的 `scripts/release-notes.sh` 会把对应小节原样写进 GitHub Release 说明；
跨版本补发时（上一个 Release 之后攒了好几版）会把中间几节一起带上，
找不到小节时退回「相较上一个标签的提交记录」。

## v0.4.1

**应用内更新**

- 修复 **「检查更新」一直报 404**：根因不在代码，在仓库可见性——GitHub 私有仓库的 Releases API 与安装包下载地址对外人一律返回 404，App 里看到的「检查更新失败 HTTP 404」其实就是「这个仓库你不让公开看」。仓库转为公开后，匿名检查、拿 Release 说明、下载 apk/hap 全部恢复正常
- 改动 **检查失败的提示按状态码分开了说**：404 直接写「GitHub 上找不到这个发布，仓库或 Release 被设成私有时就会出现」；403/429 是 GitHub 对匿名请求的限流（60 次/小时），提示会带上「大约 N 分钟后再试」；其余仍报 HTTP 状态码。以前这三种在手机上都是同一句「检查失败」，谁也不知道该干什么

## v0.4.0

这一版把「谁说了算」和「谁不玩了」两件事补齐了：空间里终于有了成员管理的面板，家人也能自己退出而不必找创建者删文件。

**空间成员与角色**

- 新增 **退出空间**：空间卡片上一颗「退出」，弹窗确认后才动手。可以选择「同时删除我在该空间创建的日程」——给自己名下的日程写墓碑，其他成员同步后就看不到了；不勾则只把自己标成已退出，日程原样留着。本机副本一定等云端写成功之后再清，离线时会明确报错说「网盘没连上，云端还不知道你退出了」，不会发生本机已经没了、别人那边从没听说过的情况。以后重新输邀请码还能进来，人和日程原样回归
- 新增 **成员管理面板**：空间卡片点「成员」，按 创建者 → 管理员 → 成员 → 已退出 排开，一人一张卡片。创建者看不到「退出」（该走「☁ 清空」），提示语会把这条边界写在脸上
- 改动 **成员卡片改成两行**：第一行是 色点、昵称（自己标「（我）」）、角色徽章、名下日程条数与加入日期，第二行才是「设为管理员 / 取消管理员 / 移出」这些操作按钮并靠右。原先挤在一行时，昵称一长就会被右边的徽章和按钮压到看不见（真机上就有一个人整条名字都没了）；现在昵称再长也只在自己的格子里向右滑动看全，徽章和日期一律不打折、不折行
- 新增 **管理员角色**：与创建者用同一个网盘账号的人（也就是拿配置码进来的家人）自动算管理员，可以改名、可以移出普通成员。判定靠成员记录里的 `davId`（网盘账号地址 + 用户名的哈希，云端不落账号原文）。管理员动不了创建者，也动不了同为管理员的人——同一个网盘账号本来就是一家人
- 新增 **被移出时本机收到通知**：对方下次同步会看到「你已被移出某某空间」，可以选择把本机副本一起清掉；本机有待写入改动时会先警告会丢，绝不静默删任何东西
- 改动 **退出/被移出的人在别人名单里怎么走，按「TA 的日程还在不在」分两种**：日程还留着的，就一直正常显示在名单里（日历上那些色块总得说清是谁写的），并且**绝不弹提示**——以后 TA 那边再怎么动，都不该再打扰这个空间里的其他人；日程已经清空的，第一次打开成员面板仍画一行「已退出/已被移出」，第二次只弹一句「某某 已退出，日程已清空」，第三次起连这句也没有了。这笔「为 TA 打开过几次」记在本机（键 = 空间 + 成员），云端那条成员记录和 TA 的历史日程一个字都不动
- 新增 **创建者可以手动指定管理员**：成员行上多一颗「设为管理员」，点了对方立刻能改空间名、移出普通成员；再点一次「取消管理员」退回普通成员。这颗按钮只在自己的角色是创建者时出现
- 改动 **管理员资格以创建者为准**：创建者能撤销任何人的管理员（包括靠「同一个网盘账号」自动认定的那位），也能把管理员一并移出空间；撤销时在成员记录写 `admOff` + `admOffBy`，此后 `davId` 相同不再自动恢复、重粘配置码也恢复不了，只有创建者重新指定才算数。管理员（非创建者）依旧只能动普通成员，给不了也收不回任何人的资格
- 新增 **被移出的人下次进入空间先撞一条告知**：点进那个空间会弹「您已被移出」，看完自动退出——本机副本清掉、首页与设置-空间管理的列表同时少一条（网盘上别人那份数据一个字不动）。同步时发现被移出走同一条流程；本机还有没同步出去的改动时，文案里会明确警告会一起丢掉
- 改动 **移出成员时也能选择是否连 TA 名下的日程一起清掉**：移出借用「退出空间」那个弹层，标题、正文、按钮文字与勾选框都换成对应语气（「同时删除 TA 在该空间创建的日程（N 条，其他成员也会同步看不到）」），两件事在界面上彻底对齐
- 改动 **先输邀请码加入、事后才粘配置码的人**：本机这个空间已经在同步的话，就保持本机绑定的网盘账号继续同步，不再弹一次改绑询问、也不会多出第二个同名空间；同时按「拿得到配置码＝与创建者同一台网盘账号」把自己认作管理员，不必等 `davId` 比对（换过设备就可能对不上）
- 改动 **首页「切换空间」的每张空间卡片也有「成员」入口**：看成员名单不是管理权限，不是管理员的人一样能看（只读，没有移出/清空那些按钮），首页与设置页的空间管理不再一边缺一块
- 改动 **配置码处补了一句写给管理员看的说明**：导入与生成两处都写明「用配置码进来的人会被视为管理员，可以改空间名、移出普通成员；只想让 TA 看日程请只发邀请码」
- 改动 **退出与移出都只打标记，不删数据**：成员记录写 `out` 时间戳和 `outBy`（自己标的显示「已退出」，别人标的显示「已被移出」），名片和名下日程全部保留。网盘没有服务器，被移出的人再输一次邀请码仍然能回来——这句直接写在面板的提示里，不糊弄用户

**设置-外观与开屏**

- 改动 **「深色模式」「默认视图」两枚小标题与组标题统一**：12px / 字重 700 / 弱文字色 / 1px 字距，和「个人资料」「账户」那几组一模一样；「默认视图」原来跟着正文走 15px，看着像另一套字体
- 改动 **深色模式一行的上方补了小标题「深色模式」**：以前三颗按钮光秃秃地挂着，跟下面的「默认视图」分不清谁是谁
- 修复 **勾选「日视图」后「跟随上次」被挤到折行**：分段按钮现在各自不折行、整组在一行内等分，390px 宽的屏上四颗（跟随上次 / 月 / 周 / 日）实测同一行
- 改动 **开屏标语里的逗号换成间隔号**：「多人共享 · 坚果云同步，无需注册」→「多人共享 · 坚果云同步 · 无需注册」，一行里三件事的隔断方式终于一致

## v0.3.0

这一版集中处理「多人多网盘」这一类纠缠在一起的问题：同一个邮箱在两台手机上终于是一个人而不是两个人，家人的配置码不再把本机网盘账号顶掉，月历格子里那几个数字也各自说清了自己的意思。

**网盘账号与配置码**

- 改动 **配置码只作用于它那一个空间，不再覆盖本机网盘账号**：以前粘家人的配置码等于把「设置-网盘同步」里的账号整个换掉，自己原来的空间立刻读不到（表现为同步不上新加入的人），严重时还会在别人的网盘里另建一份同名文档，两个人从此各写各的。现在网盘账号是**按空间记**的：设置页那台是「本机默认账号」，新建空间落在它上面；配置码只把它的空间绑到发码方的账号。两个账号冲突时（这个空间原来已经绑在别处）会弹层问你保持原样还是改用配置码里的
- 新增 **每个空间显示它住在哪个网盘**：切换空间与设置-空间管理的列表里标出「网盘 账号」，出问题一眼看得出是哪一台；本机默认账号本身就不用标
- 新增 **设置-空间管理的「扫描网盘找回我的空间」**：换设备、清过数据、或者只用配置码进来过的人，可以按成员表把网盘上属于自己身份的空间找回来，本机已知账号会逐个试（失败的账号跳过而不是整体报错）

**成员与身份**

- 新增 **同一邮箱的多台设备并成一个用户**：登录成功后自动扫网盘，凡成员表里有这个邮箱的空间都补回本机列表并绑好对应网盘账号；账户那边已有昵称、且和这台手机上的不一样时，弹层问你用哪一个（也可以自己填），选定后统一写回所有空间的成员记录，不会只改当前空间
- 修复 **同一个账号仍然显示成多个成员标签**：上一版靠「同设备 + 同昵称」折叠，但昵称改过、或折叠之后别人的本机留档把旧键补回来，就又冒出第二条。现在折掉/迁走的旧键会在云端文档里留下「旧键 → 新键」的退休记录（`retired`），归属判定、成员表收口、日程改写全部顺着它走，留档也不会再复活已退休的人
- 改动 **认人的证据分级**：两边都登录过邮箱时只认邮箱（同昵称的同桌不会被误并）；否则同设备 + 同昵称（共用平板的两个人设备号相同，靠昵称分开）；再否则退回老数据的同昵称 + 同颜色。判定归属时本机历史身份键与退休前的旧键一律算自己，不会出现「标签看着是自己、详情里却锁死不能删」
- 改动 **退出登录不再把邮箱身份改回设备号**：别的设备可能正用这个邮箱写日程，把 `u:邮箱` 那条成员改名回本机设备号等于把别人的成员条目抢过来，表现就是同一个账号又显示成两个。这台设备上此前写下的日程靠历史身份键照样认作自己的

**同步与月视图**

- 修复 **混合网盘账号的空间同步不上新加入的成员**：读到 404（换错账号、目录不对、服务端抖动）时不再无条件把本机数据覆写出去，只在确实有待写入内容时用「仅新建」方式回写，被 412 挡下就下一轮重拉合并；连续两轮 404 才判定空间被删除
- 新增 **同步异常在界面上说清楚**：顶栏同步状态与空间列表会显示「待写入」「网盘上找不到这个空间的文件（账号 ×）」等具体原因，不再只是转圈或静默失败
- 修复 **月视图格子里的点和「+N」对不上实际日程数**：以前点按人去重、`+N` 却按事件条数算，两个人写 6 条就显示成「2 个点 +4」。现在含义统一并写在同一格子里：**一个色块是一个人**（同一人当天多条只占一块，多条时块上拖一层叠卡影子），**块下的「N条」是当天日程总条数**，**「+N」是还有几个人没画下**（最多画 4 块），长按/悬停可看「谁几条」。去重键也从颜色改成成员，两人用同一颜色不会再丢点；横屏等矮屏空间不够时只保色块、不显示条数


## v0.2.6

这一版发的是 v0.2.5 那个安装包之后攒下来的全部改动：多人共用一个配置码时成员和日程归属终于对得上了，视图也听得懂手势了，鸿蒙读系统日历这件事也把平台的权限边界摊开说清楚了。

**成员与数据**

- 修复 **同一个人显示成两个成员标签**：换过身份键（登录/退出邮箱、设备号被原生存储接管、网页存储被系统清过）会留下旧键那一条，旧键名下没有日程，看着就像空间里多了个人。现在按「同设备 + 同昵称」合并成一条（老数据没设备号时按同昵称 + 同颜色判），只折掉名下零日程的重复项，本机自己那条绝不动，两条都有日程的也不动
- 修复 **自己建的日程点开却提示「别人创建的无法删除」**：同一个人换过身份键后，旧键名下那几条就成了「不是我的」，标签看着是自己、详情里却锁死。现在本机历史身份键一律算自己（旧键名下的日程还带设备号的话，同步时直接并到当前身份上），改、删、批量管理都按这个判定；空间改名权限同理

**视图与手势**

- 新增 **周视图一屏四格 / 七格切换**：小时列上方原来的「时」位置换成展开收起按钮，点一下把整周压进一屏（列宽按实测宽度算，不会多出几像素的横向滑动），再点恢复一屏四格，偏好会被记住；平板宽度仍是整周平铺，该位置保持显示「时」
- 新增 **日/周/月视图空白处横滑切换**：向左滑进一层（月→周→日），向右滑退一层，不必每次去点顶栏的视图切换钮；周视图「一屏四格」时横向本来就是时间轴自己在滚，这一手势让给滚动（滑到边界再滑才切视图），原有的月视图上滑收起/下划展开不受影响
- 新增 **设置-外观的「日视图」开关，默认关闭**：不常用的人视图条上不再出现「日」这一格，周视图点表头也不会误入；打开开关后功能照旧，若当前正停在日视图则自动回到周视图
- 新增 **设置-外观的「默认视图」**：可选 跟随上次 / 月 / 周 / 日，决定每次进入空间时停在哪个视图，点选后立刻切过去给反馈；「日」只在日视图开关打开时出现，关掉开关会自动退回「跟随上次」。这一行是左边「默认视图」标题、右边「日视图 + 勾选框」，两件事共用一行，设置页更短
- 修复 **切换空间面板点「返回」跳到了另一个空间**：直接点返回就应该留在当前空间，只是关掉面板（原来会回退到上一次进入的空间，这个「回退栈」语义已被移除）
- 新增 **空间被创建者删除时的提示**：使用者下次同步会看到「该空间已经被创建者删除」，确认后连本机副本一起移除（要连续两轮都拉到 404 才判定，换网盘账号、网络抖动不会误删；本机还有待同步改动的空间也不会被误判）

**应用内更新**

- 修复 **应用内更新只有「检查到新版本」却下不动也装不了**：下载与安装两个按钮是用 `.hidden` 类藏起来的，代码却只切了 `hidden` 属性，类名一直留在按钮上，所以它们永远显示不出来。现在按钮正常出现，进度百分比直接采用原生侧算好的值
- 修复 **旧版本残留的安装包会被当成「已就绪」去装**：只有下载包的版本和最新一致才算就绪，过期的自动清掉重新下
- 修复 **每点一次下载就多挂一个进度监听**（进度会重复回调、越点越乱），现在只绑一次

**鸿蒙 · 系统日历**

- 改动 **「直接读取系统日历并导入」不再只翻应用自己的那本日历**：原生侧改为遍历设备上所有可读日历（`getAllCalendars()`，拿不到时回退默认日历），每个日历单独容错、跨日历按事件 id 去重，并把「扫了几个日历、各读到几条、哪一个报什么错」一路带回界面。读不到东西时提示直接把这几项摊开写明，不用来回猜真机现场
- 说明 **鸿蒙上读不到你在系统日历里手动建的日程，是平台权限限制而不是 bug**：官方权限表规定 `READ_CALENDAR` 的读取范围是「本应用创建的日程」，要读别的日历得 `READ_WHOLE_CALENDAR`，那是受限开放权限（API 20 起才对普通应用开放，且要签名 profile 带 ACL）。导入弹窗在鸿蒙上会把这事写明白，并指向同一个弹窗里的「从 .ics 文件导入」这条备用路径
- 新增 **CI 的可选实验构建**：手动运行鸿蒙工作流时勾选「尝试读取全部日历」，会以 API 20 的 SDK 构建并在清单里声明该受限权限，产物单独命名、不参与发版（profile 没带 ACL 时这个包装不上，故与正式通道隔离）
- 改动 **鸿蒙包名从 `com.timemaster.app` 换成 `top.timemaster.app`**（Android 包名不变）。代价要清楚：包名一变，系统就当它是**另一个应用** —— 老包不会被覆盖，得先在新包里用配置码重新加入空间，确认数据都在之后再手动卸载老包；本机存的设备号/历史身份键也随老沙箱一起没了，新包首次会以新身份登记成员，若成员名单里出现同名的两条先别删，等同步收敛后再看是否需要合并。签名侧：AGC 的应用/Profile 是按 bundleName 绑定的，换名后要在新包名下重新出证书与 Profile，否则装不上（报包名与 profile 不匹配）

## v0.2.5

这一版修的是多人共用一个配置码时的成员与数据同步，另外鸿蒙不再每开一次就多一个后台卡片。

- 修复 **三个人（及更多）用同一个配置码时，最后加入的人「查无此人」**：不创建日程就看不到他的成员标签、空间里也一直显示 2 人。根因是网盘没有事务，谁写回都是整篇 JSON 覆盖，只要有一端拿着过期的文档写回，就会把别人刚加进去的成员（连带日程）抹掉，而被抹掉的人自己并不会补回来
- 改动 **同步引擎：成员名单只增不减 + 自愈**。本机记一份「见过的成员」留档，每次合并都补回去；发现自己不在云端名单里就自动重新登记并写回，被旧版本覆盖掉的人一两轮同步内就会回来
- 改动 **写回云端前一定先拉一次全量再合并**（之前只在拿不到 etag 时才这样），不再拿 304 的缓存整篇覆盖别人的数据
- 修复 **创建空间时邀请码撞车会把别人的空间覆盖掉**：现在改为仅新建，撞到就提示重新创建
- 修复 **鸿蒙端每点一次桌面图标就多一张最近任务卡片**：`EntryAbility` 改为单实例（`launchType: singleton`）

## v0.2.4

这一版是按真机截图逐项微调的：手机横屏终于能看到时间轴了，选择框也找回来了。

- 改动 **日视图表头压成一行**：`今天` 徽标 + `9月23日` + `周三` 横排，设备太窄放不下时自动折成两行（周视图仍是「星期在上、日期在下」）
- 修复 **手机横屏时底部留白过大、时间轴几乎看不见**：时间轴模式下外层预留的高度从 96px 压到 54px，正好贴着横屏变矮后的底栏，可视时间多了约五分之一
- 改动 **月历「收起/展开」按钮改成纯图标**（`⌃` / `⌄`），去掉文字后按钮从 108px 缩到 40px
- 删除 **设置-空间管理里的「进入」按钮**：整张卡片本来就可点，按钮是多余的
- 修复 **批量管理的复选框在深色模式下完全看不见**：全局 `input` 规则清掉了原生外观，现在整个框自己画（描边 + 勾选态填主色 + 对勾）
- 修复 **批量管理里选择框和日程文字挤成两行**：根因是 `.modal label` 优先级高于 `.batch-row`，把行方向覆盖成了 `column`，选择器带上 `.modal` 后压回一行；标题过长不再截省略号，改为居左、可向右滑动看全
- 改动 **批量管理不显示滚动条**：列表竖向和标题横向都只留滑动手势，不给滚动条留宽度
- 删除 **三处冗余说明文字**：网盘「测试连接」下方的路径说明、系统日历按钮下方的用法说明、关于-版本下方的署名说明
- 改动 **空间卡片改为两行**：第一行「房间名称 + 使用中」（名称单行超出省略），第二行才是「☁ 清空 / 移除」按钮
- 删除 **顶栏最右边的圆形「↩」按钮**（v0.2.3 加的那个）：切空间面板里的「返回」仍然回到上一个空间，功能没丢，只是不占顶栏一格
- 改动 **周视图打开即把今天滑到最左**：今天那一格自动贴到小时列右边第一格（周四就滑到周四打头），今天正好是周日时本就在第一格、不滑动。顺带修了冷启动时空间条量不到宽度导致定位失效的问题（先显示屏幕再渲染日历）
- 改动 **日历页顶部空间条分两行**：第一行只有空间标题，第二行是「邀请码 点击复制」+「同步」+「同步中…」三样并排；标题再长也只省略自己，不再把按钮挤窄。手机横屏（矮屏）仍收回一行，免得把刚还给时间轴的高度吃掉
- 改动 **平板（≥600px）日/周视图行高 58px → 78px**：宽屏上不再一行日程都塞不下

## v0.2.3

周/日时间轴这一版重点是「看得下、滑得动」，另外把系统日历回写的归属标清楚了。

- 改动 **写入系统日历的日程注明创建者**：标题后缀 `（成员名）`，描述里另起一行 `[Reunion] 创建者：成员名`；再从这个系统日历导入时按这枚标记跳过，不会把我们自己写回去的条目又抓回空间里
- 改动 **周视图一屏四天**：每格宽度按 `(屏宽 - 小时列) / 4` 计算，每小时行高 46px → 58px，横向滑动仍能看到完整七天
- 修复 **手机端周/日视图第二次进入划不到顶、星期表头被吃掉一半**：时间轴模式下外层 `#calMain` 是 `overflow:hidden`，之前却被程序滚到了当前小时，一旦滚走就再也滑不回来；现在外层强制归零，只让时间轴自己滚
- 新增 **日/周视图的「全天」行跟星期表头一起吸顶**：滚动 24 小时刻度时，全天日程和班休标记不再跟着滑走
- 新增 **时间标尺上显示精确到分钟的当前时间**：小时列是吸左的，横向滑动时也不会丢，每分钟自己刷新
- 改动 **周视图标题的日期区间拆两行**：第一个日期和 `–` 一行、第二个日期一行（日视图仍是一行），不再被顶栏挤断
- 修复 **切换空间的「返回」会掉回开屏页**：只有一个空间时改为留在当前空间；顶栏「☰」旁加了同款圆形「↩」按钮，只有存在上一个空间时才出现
- 改动 **设置-空间管理**：「使用中」徽标右对齐，邀请码那行的「码」字去掉
- 新增 **月视图可折叠**：手机上滑收起整月只留选中那一行、下滑展开，日历下方加了「⌃ 收起 / ⌄ 展开」按钮；平板宽度不折叠
- 改动 **成员条压成一行**：人多时横向滑动而不换行，不再竖向挤占日历空间

## v0.2.2

鸿蒙写系统日历的报错定位到了，另外把返回手势、自动旋转和日程编辑补齐。

- 修复 **鸿蒙端导入/回写系统日历报 `[23900001] invalid arg[0], i.e. invalid keys!`**：CalendarKit 的 `EventFilter` 是个带静态工厂的类，原生侧按包装对象解参，之前直接传 `{ timeRange, calendarType }` 对象字面量必然失败；改成 `calendarManager.EventFilter.filterByTime(起, 止)`
- 改动 **日/周视图的日程色块显示更多文字**：字号调大，仍是标题在前、时间在后，文字超出块高度时在块内上下滑动即可看完；一小时以内的短日程改成「标题 · 时间」同行挤着显示
- 新增 **自己的日程可以编辑**：详情弹窗在「删除」旁加「编辑」，改标题/日期/时间/类型/重复/地点/备注都是原地更新那一条（不会再冒出一条新日程），把「重复」改回不重复也会真的清掉循环规则
- 修复 **输入法收起后底部导航栏不回来**：鸿蒙收起输入法时不一定派发焦点事件，改成按可视视口被压矮与否判断，键盘一退底栏立刻恢复；转屏不会被误判成键盘
- 新增 **全面屏手势返回逐层回退**：侧滑/返回键依次关掉弹窗、退回日历页，只有在首页再返回才退出应用（Android 与鸿蒙都接上了）
- 改动 **手机端适配自动旋转**：鸿蒙 ability 明确为跟随系统旋转开关，Android 侧同样显式声明，横屏布局沿用上一版的适配
- 改动 **月视图日程卡列表的成员名尽量完整显示**（名称列 52px → 92px），右侧时间跨度条相应收窄压矮

## v0.2.1

平板与周视图的收尾，外加鸿蒙的深色模式。

- 修复 **鸿蒙端「深色模式跟随系统」无效**：ArkWeb 的 Web 组件默认不带网页深色模式，网页里的 `prefers-color-scheme` 永远收到浅色，改成跟随系统（`WebDarkMode.Auto`）后才真正联动；同时补了深色的启动窗口底色与 Web 组件底色，旋转/缩放时不再露出白边
- 改动 **周视图一格放大**：手机上一次显示 5 天（原来 7 天挤在一起放不下日程标题），左右滑动看满七天；小时列吸在左侧、日期表头吸在顶部，横着滑也不会看不出现在是几点
- 改动 时间轴改由自己滚动，右下角「新建」悬浮按钮不再白吃掉一截高度
- 修复 **平板竖屏/横屏月视图左右错位**：右侧日程卡列表会把左边的月历整块顶下去，现在两栏顶边对齐
- 改动 **月视图格子大小自适应**：宽屏下六行按剩余高度均分，平板上格子明显变大
- 改动 宽屏内容宽度上限 680px → 900px（≥1000px 时为 1120px）

## v0.2.0

界面重做的一版，日历终于像个日历了。

- 新增 **日 / 周 / 月三种视图**，选择会被记住，下次打开还是它
  - 月视图：格子只留日期与成员色块，一屏放得下整月；点某天在下方展开当天的日程卡列表（创建人、时间、地点、备注，按时间先后），列表顶部是「每人一条时间跨度轨」，谁排到几点一眼可见；「新建日程」收进列表最下面
  - 周视图：7 列时间轴，重叠日程自动分栏并排
  - 日视图：单列时间轴 + 全天区 + 当前时间红线（每分钟自己走）
- 新增 **横屏适配**：顶栏、格子、时间轴行高都按横屏压缩，不再是竖屏内容硬旋转
- 新增 **冷启动回到上次进入的空间**（首次安装仍进引导页）
- 改动 顶栏空间条：空间名加大、同步状态移到标题下方、同步按钮靠最右、邀请码一行显示；点空间名即可在弹出列表里快速切换空间（列表末尾含新建空间）
- 改动 切换空间面板与设置页功能对齐，可直接新建空间或用邀请码加入；「返回」回到上一次进入的空间
- 改动 设置页的空间卡片点一下就能切过去
- 改动 底栏「房间」更名为「空间」
- 修复 底栏遮挡主界面内容
- 移除 鸿蒙版的应用内更新入口（HAP 无法自装，留着只会误导）

## v0.1.5

- 新增 应用内更新：检查 GitHub Releases → 原生后台下载 APK → 唤起系统安装，下载进度可见
- 改动 月视图格子放大，日程标题可显示多行
- 修复 设备身份在清缓存后漂移的问题（身份改由原生侧持久化）
- 修复 同步状态误报

## v0.1.4

- 修复 鸿蒙创建空间 / 连接网盘失败（空响应体时 base64 解码崩溃）
- 新增 鸿蒙导航条沉浸式

## v0.1.3

- 修复 鸿蒙桥自检误报「桥未注入」（`runJavaScript` 返回值带 JSON 引号未剥离）

## v0.1.2

- 修复 鸿蒙 `registerJavaScriptProxy` 晚于首屏加载导致桥不可用（自检后自动重载）
- 修复 Android 主题图标缺省
- 改动 键盘弹起时底栏行为

## v0.1.1

- 新增 日程批量管理与多选删除
- 新增 网盘数据一键清空（带二次确认）
- 新增 邮箱验证码绑定到当前本地账户
- 修复 切换空间面板「返回」未回到上一次空间
- 新增 鸿蒙端启动自检浮层

## v0.1.0

- 更名 Reunion，重做图标与底栏药丸样式
- 修复 系统日程重复导入（按 `sourceUid` 去重 + 历史重复自动清理）

## v0.0.x

- 核心：坚果云 WebDAV 同步（免服务器）、多空间、成员标签色、循环日程（RRULE）、.ics 导入
- 双端壳：Capacitor Android APK + HarmonyOS NEXT HAP，全部由 GitHub Actions 产出
- 系统日历双向同步、深色模式、可选邮箱登录
