{
  "name": "timemaster",
  "version": "0.1.1",
  "private": true,
  "description": "Reunion — 多人共享日程（Android APK / HarmonyOS NEXT HAP · 坚果云 WebDAV 同步）",
  "scripts": {
    "start": "node server.js",
    "sync": "npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
